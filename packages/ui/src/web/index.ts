/**
 * `@odasy/ui/web` — public surface for web React applications.
 *
 * Pairs with the canonical theme tokens from `@odasy/ui/theme` and the
 * generated CSS at `@odasy/ui/styles/tokens.css`.
 */

export * from './components';
export * from './theme';
